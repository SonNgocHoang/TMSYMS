<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tms.com.libre.tms">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.SET_TIME_ZONE"
        android:protectionLevel="signature|system" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity
            android:name=".AcStart"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AcMain" />
        <activity android:name=".AcLogin" />
        <activity
            android:name=".AcCalendar"
            android:theme="@style/AppTheme">

            <!-- Set parent for home button -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AcMain" />
        </activity>
        <activity android:name=".AcTruckLoadInfo" />
        <activity android:name=".AcPickup" />
        <activity android:name=".AcOnRoute" />
        <activity android:name=".AcLoginYms" />
        <activity android:name=".AcMainYms" />
        <activity android:name=".XzingScanner" />
        <activity
            android:name=".AccAddNote"
            android:theme="@style/AppTheme" />
        <activity android:name=".AcDrop"></activity>
    </application>

</manifest>